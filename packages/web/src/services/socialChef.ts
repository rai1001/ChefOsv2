export const generateMarketingContent = async (
  recipeName: string,
  imageUrl: string,
  businessType: 'HOTEL' | 'RESTAURANT' = 'RESTAURANT'
): Promise<SocialChefResult> => {
  throw new Error('Social Chef AI generation is temporarily disabled.');
};
