import { useEffect } from 'react';

export const useNotificationSubscription = () => {
  // Stubbed for migration
  useEffect(() => {
    // Future: Implement Supabase realtime subscription
    return () => {};
  }, []);
};
